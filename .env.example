# Google Search Console MCP Server Configuration

# FastMCP Google OAuth Configuration
# Get credentials from: https://console.cloud.google.com/apis/credentials

# Use the Google OAuth provider
FASTMCP_SERVER_AUTH=fastmcp.server.auth.providers.google.GoogleProvider

# Required: Google OAuth Client ID
FASTMCP_SERVER_AUTH_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Required: Google OAuth Client Secret
FASTMCP_SERVER_AUTH_GOOGLE_CLIENT_SECRET=GOCSPX-your-client-secret

# Optional: Base URL for OAuth callbacks (default: http://localhost:8000)
# FASTMCP_SERVER_AUTH_GOOGLE_BASE_URL=http://localhost:8000

# Optional: OAuth redirect path (default: /auth/callback)
# FASTMCP_SERVER_AUTH_GOOGLE_REDIRECT_PATH=/auth/callback

# Required: OAuth scopes (comma-separated)
# For full access to Google Search Console:
FASTMCP_SERVER_AUTH_GOOGLE_REQUIRED_SCOPES=openid,https://www.googleapis.com/auth/userinfo.email,https://www.googleapis.com/auth/webmasters

# For read-only access, use:
# FASTMCP_SERVER_AUTH_GOOGLE_REQUIRED_SCOPES=openid,https://www.googleapis.com/auth/userinfo.email,https://www.googleapis.com/auth/webmasters.readonly

# Optional: OAuth timeout in seconds (default: 300)
# FASTMCP_SERVER_AUTH_GOOGLE_TIMEOUT_SECONDS=300
